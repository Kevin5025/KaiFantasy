using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public interface IActivator : IHealthStateBody, IComponent {
	//bool Activate(IActivatable activatable, Dictionary<object, object> argumentDictionary = null);
}
